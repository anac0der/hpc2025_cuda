# CUDA task at HPC course (sequential code branch)
Sequential code build:
```
make -f make_seq
```
Run:
```
./run_seq 256 50 0.002
```
Here `256` is the number of grid nodes, `50` is the number of iterations and `0.002` is the time step.
